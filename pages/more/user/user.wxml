<!-- 导入模板 -->
<import src="/template/list/list.wxml" />

<!-- 用户信息 -->
<view class="userinfo">
  <template is="basic-list" data="{{lText: '头像', avatar: userInfo.avatar}}" />
  <template is="basic-list" data="{{lText: '昵称', rText: userInfo.nickname}}" />
  <template is="basic-list" data="{{lText: '姓名', rText: userInfo.name}}" />
  <template is="basic-list" data="{{lText: '学号', rText: userInfo.studentId}}" />
  <template is="basic-list" data="{{lText: '寝室', rText: userInfo.dormitory}}" />
  <template is="basic-list" data="{{href: '/pages/more/binding', lText: '账号绑定', rText: bind ? '已绑定' : '未绑定'}}" />
</view>
