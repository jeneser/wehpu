<view class="binding">
  <!-- 第一步 -->
  <!-- <view class="step-one">
    <view class="form-one">
      <view class="input-group">
        <input class="input-group__input {{userIdFocus ? 'input-active' : ''}}" id="userId" type="number" maxlength="12" confirm-type="done" auto-focus="true" placeholder="学号或一卡通号" bindfocus="inputFocus" bindblur="inputBlur"></input>
      </view>
      <text class="form-one__tip">接下来进行VPN认证和教务认证,离完成还差两步</text>
      <view class="form-one__btns">
        <text class="form-one__btn--cancel" bindtap="navigateCancel">暂不认证</text>
        <text class="form-one__btn--next" bindtap="navigateNext">下一步</text>
      </view>
    </view>
  </view> -->
  <!-- 第二步 -->
  <!-- <view class="step-two">
    <view class="form-two">

        <view class="input-group">
        <input class="input-group__input {{vpnPassWordFocus ? 'input-active' : ''}}" id="vpnPassWord" type="password" confirm-type="done" auto-focus="true" placeholder="校外访问VPN密码" bindfocus="inputFocus" bindblur="inputBlur"></input>
        <input class="input-group__input {{jwcPassWordFocus ? 'input-active' : ''}}" id="jwcPassWord" type="password" confirm-type="done" placeholder="教务处密码" bindfocus="inputFocus" bindblur="inputBlur"></input>
      </view>
      <view class="form-two__tip">
        <text>距离大功告成只差一步, </text>
        <text class="form-two__help" bindtap="showHelp">需要帮助?</text>
      </view>

      <view class="loading">
        <view class="loading__main"></view>
        <text class="loading__text">认证中</text>
      </view>
      <view class="form-two__btns">
        <text class="form-two__btn--start" bindtap="navigateNext">开始认证</text>
      </view>
    </view>
    <view class="help" id="help" wx:if="{{helpStatus}}" bindtap="hideHelp">
      <view class="help__box">
        <image class="help__close" id="close-help" src="/images/ic_clear.png" bindtap="hideHelp"></image>
        <view class="help__item">
          <text class="help__question">1.校外访问VPN密码是什么?</text>
          <text class="help__answer">初始密码或者已更改的密码。初始密码一般为身份证号后6位。</text>
        </view>
        <view class="help__item">
          <text class="help__question">2.教务处密码是什么?</text>
          <text class="help__answer">初始密码或者已更改的密码。初始密码为学号/一卡通号。</text>
        </view>
        <view class="help__item">
          <text class="help__question">3.忘记密码?</text>
          <text class="help__answer">请联系学校教务处管理员。</text>
        </view>
      </view>
    </view>
  </view> -->
  <!-- 认证成功 -->
  <view class="succeed">
    认证成功
  </view>
</view>
